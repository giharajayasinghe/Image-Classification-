# Image-Classification-
A simple deep learning project that classifies facial images as Happy or Sad using a Convolutional Neural Network (CNN) built with TensorFlow/Keras. The model is trained on image data and performs preprocessing, training, validation, and prediction for emotion classification.
